import { LBG } from "#theme"
import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  title: {
    height: 47,
  },

  divisionLine: {
    height: 2,
    backgroundColor: LBG,
  },
})
