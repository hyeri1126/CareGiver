import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  submitBtn: {
    // position: "absolute",
    // bottom: 0,
    marginTop: "auto",
    marginBottom: 8,

    alignItems: "center",

    width: "100%",
    borderRadius: 8,

    paddingVertical: 18,
  },
})
