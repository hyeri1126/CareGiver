import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  reviewContainer: {
    flexDirection: "row",
    alignItems: "center",

    // marginTop: 4,
  },
  star: {
    width: 13,
    height: 12,
  },
})
