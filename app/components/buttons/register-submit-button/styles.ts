import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  submitBtn: {
    marginTop: "auto",
    marginBottom: 8,

    alignItems: "center",

    width: "100%",
    borderRadius: 8,

    paddingVertical: 18,
  },
})
