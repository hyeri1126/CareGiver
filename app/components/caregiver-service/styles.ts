import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  root: {
    width: "auto",
    height: "auto",
    // height: 28,
    // backgroundColor: "yellow",
  },
  emoji: {
    marginBottom: 4, //! 이모지 정렬 고육지책
    // paddingBottom: 4,
    // lineHeight: 28,
  },
})
