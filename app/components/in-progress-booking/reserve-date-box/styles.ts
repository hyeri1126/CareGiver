import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  dateBox: {
    flexDirection: "column",
    alignItems: "center",
  },
  arrow: {
    width: 16,
    height: 16,

    marginHorizontal: 36,
  },
})
